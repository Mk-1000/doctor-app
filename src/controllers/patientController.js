const BaseController = require('./baseController');

class PatientController extends BaseController {
  constructor(service) {
    super(service);
  }
  
  // Add patient-specific controller methods here if needed
}

module.exports = PatientController;